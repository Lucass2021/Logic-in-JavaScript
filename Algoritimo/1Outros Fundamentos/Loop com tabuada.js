// Pedir para o usuário digitar um número
// e mostrar a tabuada desse número de 1 1000


//Estudar loops, for and while

let number = parseInt(prompt("Digite um número"));

for (i = 1; i <= 1000; i++) {

    console.log(`${number} * ${i} é igual a ${number * i}`)
}
